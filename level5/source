#include <stdio.h>
#include <stdlib.h>

void	o(void)
{
	system("/bin/sh");
	exit(1);
}

void	n(void)
{
	char 	buff[512];				//0xbffff510

	fgets(buff, 512, stdin);
	printf(buff);
	exit(1);
}

int		main (void)
{
	n();
	return (0);
}
